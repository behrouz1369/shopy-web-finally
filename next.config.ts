import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode:false,
  images: {
    domains:['static.roocket.ir','dummyimage.com','craftohtml.themezaa.com']
  },
};

export default nextConfig;
